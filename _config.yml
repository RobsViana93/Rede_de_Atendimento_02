# Configuração do GitHub Pages (Jekyll)
# Mantém todos os arquivos como estão e garante URLs limpas.
include:
  - firestore.rules
  - firebase.json
defaults:
  - scope:
      path: "admin.html"
    values:
      permalink: "/admin.html"
